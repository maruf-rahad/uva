#include<bits/stdc++.h>
using namespace std;
int main()
{
    long long a,b,c,d,i,j,sum;
    while(scanf("%lld",&a)==1){
        if(a<0)break;
        sum=(a*(a+1))/2;
        sum=sum-3+4;
        printf("%lld\n",sum);
    }



return 0;
}
